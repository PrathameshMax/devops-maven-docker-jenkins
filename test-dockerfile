FROM tomcat:9-jdk17

ARG BUILD_VERSION

RUN rm -rf /usr/local/tomcat/webapps/*

COPY ramanbuild2/devops-${BUILD_VERSION}.war /usr/local/tomcat/webapps/devops.war

EXPOSE 8080

CMD ["catalina.sh", "run"]
